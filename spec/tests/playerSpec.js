describe("Player", function () {
    var player;

    beforeEach(function () {
        player = new Q.Player();
    });

    it("Does nothing", function () {
        expect(1).toEqual(1);
    });
});
